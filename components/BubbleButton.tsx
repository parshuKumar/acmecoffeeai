// components/BubbleButton.tsx — the floating launcher button rendered inside /embed
// when it's shown as a closed bubble state, if we go that route in Phase 5/6.
// The bubble injected into the HOST page lives in public/widget.js instead (vanilla JS,
// since it must run outside React on someone else's site) — see docs/08-phase-6-widget-loader-script.md.
